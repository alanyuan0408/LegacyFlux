Hello, <%= @user.name %>
Web Club Automated Email System. The content is generated based on your settings
To modify your settings, login to your account at www.uoftweb.com
Please email the site admin at alan[dot]yuan@mail[dot]utoronto[dot]ca for any complaints.

Summary
	<% if @mail_setting.full_time_job && !@jobs.blank?%>
		<% @jobs.each do |post| %>
			<%= post.item_title %>
		<% end %>
	<% end %>

	<% if @mail_setting.Research_job && !@research.blank?%>
		<% @research.each do |post| %>
			<%= post.item_title %>
		<% end %>
	<% end %>

	<% if @mail_setting.news && !@news.blank?%>
		<% @news.each do |post| %>
			<%= post.item_title %>
		<% end %>
	<% end %>

	<% if @mail_setting.events && !@events.blank?%>
		<% @events.each do |post| %>
			<%= post.item_title %>
		<% end %>
	<% end %>

Jobs Updates
<% if @mail_setting.full_time_job && !@jobs.blank?%>
<% @jobs.each do |post| %>
<%= post.item_title %>
<%= HTML_Truncator.truncate(Sanitize.fragment(post.item_text.strip.gsub("mail.utoronto.ca", "mail[dot]utoronto[dot]ca").gsub(/\<br\s*\/?\>/, "")), 60, :ellipsis => "... read more at http://www.uoftweb.com/feedbanks/" + post.id.to_s) %>
<% end %>
<% end %>
<% if @mail_setting.Research_job && !@research.blank?%>

Research Updates

<% @research.each do |post| %>
<%= post.item_title %>
<%= HTML_Truncator.truncate(Sanitize.fragment(post.item_text.strip.gsub("mail.utoronto.ca", "mail[dot]utoronto[dot]ca").gsub(/\<br\s*\/?\>/, "")), 60, :ellipsis => "... read more at http://www.uoftweb.com/feedbanks/" + post.id.to_s) %>
<% end %>
<% end %>
<% if @mail_setting.news && !@news.blank?%>

News Updates 

<% @news.each do |post| %>
<%= post.item_title %>
<%= HTML_Truncator.truncate(Sanitize.fragment(post.item_text.strip.gsub("mail.utoronto.ca", "mail[dot]utoronto[dot]ca").gsub(/\<br\s*\/?\>/, "")), 60, :ellipsis => "... read more at http://www.uoftweb.com/feedbanks/" + post.id.to_s) %>
<% end %>
<% end %>
<% if @mail_setting.events && !@events.blank?%>

Events Updates
    
<% @events.each do |post| %>
<%= post.item_title %>
<%= HTML_Truncator.truncate(Sanitize.fragment(post.item_text.strip.gsub("mail.utoronto.ca", "mail[dot]utoronto[dot]ca").gsub(/\<br\s*\/?\>/, "")), 60, :ellipsis => "... read more at http://www.uoftweb.com/feedbanks/" + post.id.to_s) %>
<% end %>
<% end %>

Thanks,
Web Development Club
